name: Auto Update Docs

on:
  push:
    branches: [ main, master ]
  # Deploy ulang secara manual dari tab Actions tanpa perlu commit baru.
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: 'pages'
  cancel-in-progress: true

jobs:
  deploy:
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    runs-on: ubuntu-latest
    
    steps:
      - name: Checkout Repository
        uses: actions/checkout@v4

      # 1. Jalankan Skrip Python (Membaca Preset + Memecah README jadi Multi-Page)
      - name: Split README & Apply Preset
        run: python3 documentation/dev-build.py

      # 2. Build Web Astro
      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 24
          cache: 'npm'
          cache-dependency-path: 'documentation/package-lock.json'

      - name: Build Astro Site
        run: |
          cd documentation
          npm install
          npm run build

      # 3. Publish ke GitHub Pages
      - name: Upload Artifact
        uses: actions/upload-pages-artifact@v3
        with:
          path: documentation/dist

      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v4